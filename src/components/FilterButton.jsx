import React, { useState } from 'react';
import '../styling/FilterButton.css';

const FilterButton = (props) => {
  return (
    <button className="filterButtonStyling">
      Add filter +
    </button>
  );
}
export default FilterButton;
